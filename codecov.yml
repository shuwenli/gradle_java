codecov:
  token: 0d128613-1224-4b3c-a628-24c7b2631ee7
  branch: master
coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        target: auto
        if_no_uploads: error

    patch:
      default:
        if_no_uploads: error

    changes: true

comment:
  layout: "header, diff, changes, sunburst, uncovered"
  branches: null
  behavior: default

